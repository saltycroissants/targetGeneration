# unity random target generation

unity project for VR HCI User Research.
targets are randomly generated at the same distance from a player.

target generation is done in Assets > Scripts > CreateTarget.cs
